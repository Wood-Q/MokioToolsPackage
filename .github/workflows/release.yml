# Release workflow — builds the Tauri desktop app and publishes a GitHub Release
# with macOS DMG artifact.
#
# ── Without Apple Developer secrets ────────────────────────────────────
#   The app is ad-hoc signed (signingIdentity "-").  Users who get the
#   "app is damaged" Gatekeeper error must run:
#     xattr -cr /Applications/Mokio.app
#   The release body includes this instruction automatically.
#
# ── With Apple Developer secrets ──────────────────────────────────────
#   Configure these repository secrets for signed + notarized builds:
#     APPLE_CERTIFICATE          — base64-encoded .p12 export
#     APPLE_CERTIFICATE_PASSWORD — password used when exporting the .p12
#     APPLE_API_ISSUER           — App Store Connect API issuer ID
#     APPLE_API_KEY              — App Store Connect API key ID
#     APPLE_API_KEY_PATH         — Path to the .p8 private key file
#   When present the build is properly signed + notarized (no Gatekeeper warning).

name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (e.g. v0.1.0). Defaults to the current tag."
        required: false
        type: string

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release:
    name: Build & Release macOS
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      # ── Apple code signing (only when secrets are configured) ─────────
      - name: Import Apple Developer Certificate
        if: ${{ secrets.APPLE_CERTIFICATE != '' }}
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
        run: |
          echo "$APPLE_CERTIFICATE" | base64 --decode > certificate.p12
          security create-keychain -p actions build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p actions build.keychain
          security set-keychain-settings -t 3600 -u build.keychain
          security import certificate.p12 -k build.keychain -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k actions build.keychain
          rm -f certificate.p12

      - name: Resolve signing identity
        if: ${{ secrets.APPLE_CERTIFICATE != '' }}
        run: |
          CERT_INFO=$(security find-identity -v -p codesigning build.keychain | head -1)
          CERT_ID=$(echo "$CERT_INFO" | awk -F'"' '{print $2}')
          echo "APPLE_SIGNING_IDENTITY=$CERT_ID" >> "$GITHUB_ENV"
          echo "Signing identity: $CERT_ID"

      # ── Build ─────────────────────────────────────────────────────────
      - name: Install Tauri CLI
        run: cargo install tauri-cli

      # Signed + notarized build — only when Apple secrets are present.
      # APPLE_CERTIFICATE etc. are passed as step-level env so they are
      # *undefined* (not empty strings) when the secrets are absent,
      # preventing Tauri from attempting a certificate import.
      - name: Build Tauri app (signed)
        if: ${{ secrets.APPLE_CERTIFICATE != '' }}
        run: cargo tauri build --bundles app,dmg
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_SIGNING_IDENTITY: ${{ env.APPLE_SIGNING_IDENTITY }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
          APPLE_API_KEY: ${{ secrets.APPLE_API_KEY }}
          APPLE_API_KEY_PATH: ${{ secrets.APPLE_API_KEY_PATH }}

      # Unsigned / ad-hoc build — no Apple secrets required.
      # signingIdentity "-" in tauri.conf.json ensures ad-hoc signing.
      - name: Build Tauri app (ad-hoc signed)
        if: ${{ secrets.APPLE_CERTIFICATE == '' }}
        run: cargo tauri build --bundles app,dmg

      # ── Release ────────────────────────────────────────────────────────
      - name: Determine tag
        id: tag
        run: |
          if [ -n "${{ inputs.tag }}" ]; then
            echo "value=${{ inputs.tag }}" >> "$GITHUB_OUTPUT"
          elif [ "${{ github.ref_type }}" = "tag" ]; then
            echo "value=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
          else
            echo "value=dev-$(date +%Y%m%d)" >> "$GITHUB_OUTPUT"
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.value }}
          generate_release_notes: true
          body: |
            ## Installation

            1. Download **Mokio.dmg** from the assets below.
            2. Open the DMG and drag **Mokio** to the Applications folder.
            3. If macOS shows an "app is damaged and can't be opened" error, open **Terminal** and run:
               ```
               xattr -cr /Applications/Mokio.app
               ```
               Then open the app again. (Required because the app is not notarized.)

            > **Tip:** You can also right-click the app and choose **Open** the first time.
          files: |
            target/release/bundle/dmg/*.dmg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
